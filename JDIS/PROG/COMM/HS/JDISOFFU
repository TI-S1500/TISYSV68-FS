:
# JDISOFF - HIGHSPEED MODEM PROJECT - TI UNIX VERSION ONLY
# 11/29/95 - R96-3 - remove sys000
# 01/25/96           add OFFTTY and sedoff, remove line_config
#
echo "\nThis will disable the modem for dial in support."
chmod 766 /etc/inittab
OFFTTY=tty00
export OFFTTY
/JDIS/PROG/CMN/sedoff
kill -9 `ps -t 00 | awk 'NR > 1 {print $1}'`

loop=0
while (test "$loop" -lt "9")
do
        sleep 5
        lines=`ps -t 00 | wc -l`
        if (test $lines = "1")
        then
                break
        fi
        loop=`expr $loop + 1`
done
if (test "$loop" = "9")
then
        echo "\nJDISOFF: Error while disabling the line.\n"
        exit 1
fi

echo "\nReturn your A/B box to the appropriate position."
echo "\nProcess Complete.\n"
exit 0
