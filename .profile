# .profile  script for sh login

# Set preferred control characters
stty erase '^H' intr '^C' kill '^U' quit '^\' eof '^D' echoe echok tab3
# Set up search path
PATH=/bin:/usr/bin:/etc:/util:/usr/local:.
export PATH
