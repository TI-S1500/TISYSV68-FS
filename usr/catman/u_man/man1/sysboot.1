SYSBOOT(1T)                                           SYSBOOT(1T)

NAME
     sysboot - Shuts down and resets the system

SYNOPSIS
     sysboot

DESCRIPTION
     The sysboot command is identical to the shutdown(1M) command
     except that the system does not stop in single-user mode
     after it is shut down. It reboots.

SEE ALSO
     bootdvr(1T), shutdown(1M), shutdown(2).


