ACCEPT(1M)                                             ACCEPT(1M)

NAME
     accept - Allows LP requests

SYNOPSIS
     /usr/lib/accept destinations

DESCRIPTION
     accept allows lp(1) to accept requests for the named
     destinations.  A destination can be either a line printer
     (LP) or a class of printers.  Use lpstat(1) to find the
     status of destinations.

FILES
     /usr/spool/lp/*

SEE ALSO
     enable(1), lp(1), lpadmin(1M), lpsched(1M), lpstat(1),
     reject(1M).


