UNLINK(1M)                                             UNLINK(1M)

NAME
     unlink - Exercises the unlink(2) system call

SYNOPSIS
     unlink file

DESCRIPTION
     The unlink command performs the unlink system call on file
     without error checking.  This command can be executed only
     by the super-user.

     Unlink deletes the link (directory entry) for file.

SEE ALSO
     rm(1), link(1M), link(2), unlink(2).


