LOGNAME(1)                                             LOGNAME(1)

NAME
     logname - Gets login name

SYNOPSIS
     logname

DESCRIPTION
   Purpose
     The logname command returns the contents of the environment
     variable $LOGNAME, which is set when a user logs into the
     system.

EXAMPLE
     In the following example the command returns the user's
     login name to the standard output.

     Output similar to the following is displayed:
          yourname

          logname

FILES
     /etc/profile

SEE ALSO
     env(1), environ(5), login(1), logname(3X).


